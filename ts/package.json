{
  "name": "mazegenerator",
  "version": "1.0.0",
  "description": "TypeScript port of razimantv/mazegenerator",
  "main": "dist/main.js",
  "bin": {
    "mazegen": "dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "build:web": "npx esbuild src/web.ts --bundle --format=iife --global-name=MazeGen --outfile=../maze-bundle.js --platform=browser",
    "start": "node dist/main.js",
    "test": "node dist/test-oracle.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
